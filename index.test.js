const { calculator1 } = require("./index");

describe("calculator1", () => {
    //1.1
    test("has no arguments in function parameter", () => {
        expect(calculator1).//HAS A PARAMETER OR NOT!!!!!!!!!!!
    });
});
